<Window x:Class="ClaudeTracker.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="New Profile"
        Width="380" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource MaterialDesign.Brush.Background}">
    <Grid Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" Text="Enter profile name:"
                   FontSize="14" FontWeight="SemiBold" Margin="0,0,0,12" />

        <TextBox Grid.Row="1" x:Name="InputBox"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 materialDesign:HintAssist.Hint="Profile name"
                 FontSize="13" MaxLength="50"
                 KeyDown="InputBox_KeyDown" />

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Height="34" FontSize="12" Margin="0,0,8,0"
                    Click="Cancel_Click" />
            <Button x:Name="OkButton" Content="Create"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Height="34" FontSize="12"
                    Click="Ok_Click" />
        </StackPanel>
    </Grid>
</Window>
